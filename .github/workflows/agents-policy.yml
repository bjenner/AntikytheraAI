name: AGENTS Policy

on:
  push:
  pull_request:

jobs:
  validate-agents:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate AGENTS policy
        run: scripts/check_agents_policy.sh
